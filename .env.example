# Optional for the foundation. Required before database access or migrations.
DATABASE_URL=
# Reserved for the authentication milestone; no auth routes are active yet.
AUTH_SECRET=
NEXT_PUBLIC_APP_URL=http://localhost:3000
